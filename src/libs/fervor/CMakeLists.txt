file(GLOB_RECURSE fervor_sources CONFIGURE_DEPENDS *.cpp *.c)
cmake_glob_remove_generated_sources(fervor_sources)
file(GLOB_RECURSE fervor_headers CONFIGURE_DEPENDS *.h)
file(GLOB_RECURSE fervor_forms CONFIGURE_DEPENDS *.ui)
file(GLOB_RECURSE fervor_resources CONFIGURE_DEPENDS *.qrc)

add_library(fervor STATIC ${fervor_sources} ${fervor_resources})
target_include_directories(fervor PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(fervor PUBLIC Qt6::Network Qt6::Widgets Qt6::Xml Qt6::PrintSupport ifc vmisc libs_common_include)
target_compile_features(fervor PUBLIC cxx_std_14)
